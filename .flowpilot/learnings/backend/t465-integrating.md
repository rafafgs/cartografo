### t465 (integrating, unverified)

- 'The branch is not at fault' and 'here is what is at fault' are two different claims needing separate evidence. An empty `git diff main HEAD` over the Docker path, plus a reproduction containing zero repository content, establishes only the first. I carried the first into a confident answer for the second.
