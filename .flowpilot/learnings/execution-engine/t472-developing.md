### t472 (developing, unverified)

- AC3 (a real FlowPilot alpha_test run against this bench, evidenced by stage_runs.result_json) and AC4 (founder's own `cartografo up` and this ticket's bench answering concurrently) are the ticket's own named TDD exceptions — operational, one-time, and requiring FlowPilot's external scheduler and the founder's actual long-running instance, neither of which this isolated developer session can produce. Not attempted; left for the ticket's close-out per its own Definition of Done.
