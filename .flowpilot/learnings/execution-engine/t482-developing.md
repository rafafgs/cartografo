### t482 (developing, unverified)

- The acceptance tests AT1-AT3 PASS before the implementation, and that is the honest state, not a broken red. They script what a session prints, and the Field[] wire shipped in t480 — so nothing deterministic can hold a model to the new shape (the ticket's own TDD Exceptions say so). The real red this ticket has is the hash pin: I ran it deliberately with the instructions edited and the hash stale, saw `t462 AT10` fail alone, then recomputed. Anyone re-doing this should judge red/green by that test, not by the e2e.
- Three files outside the ticket's declared surface carried the same 'one question per session' claim the skill's description carried, and were corrected with it: `factory-graphs/map-design/README.md` (a table cell quoting the skill description verbatim), `graph.json`'s own `metadata.description` (in a declared file, but a field the ticket did not name), and `docs/spec/interview.md`'s founding-requirement line at :8 and `docs/spec/screen-interview.md`'s intro at :12 (both declared files, both outside the line ranges FR8/FR10 named).
