### t492 (developing, unverified)

- Left alone deliberately, per Out of Scope: the stale doc comment at `packages/screen/src/register-map.ts:51` ('A manifest as `session.output.draft.skills[]` carries it'). Also untouched and worth a look — `docs/spec/interview.md:90-91` still states the flat shape as the only doctrine, which is now half the story for the projection even though it stays true for the registered contract.
