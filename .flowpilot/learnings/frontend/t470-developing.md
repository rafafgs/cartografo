### t470 (developing, unverified)

- The same false claim survives in three .flowpilot process files the ticket did not name: profile.yml:107 (the design-system paragraph every developer session is handed), overlays/refinement.md:12 ('which of the two stylesheets the change lands in — they share no token'), and learnings/frontend.md:9, which still tells agents to check which half their page is on. Those steer agents more directly than the spec does; they are outside docs/ and outside this ticket's declared shared-file surface, so I left them untouched — worth a ticket of their own.
