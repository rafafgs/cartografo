### t460 (developing, unverified)

- Every mid-interview draft shows a node_with_contract line PER STEP, and it is not a bug in the panel: hasSkillRef demands id+version+hash, and a draft's pins carry no hash until registration (interview.md §5) — so the panel names problems that pressing register would itself resolve (register-map.ts closes the pins before sending). Unfiltered on purpose (filtering would be a second judgement, out of scope), but it is the first thing a follow-up ticket should reconsider.
- The panel's prose says 'node' and 'edge', not 'step' — graph-soundness.js is reused verbatim per the ticket's own instruction. screen-interview.md §2's narrower vocabulary rule is only swept for job/runner/input request, so no test catches this; it is a deliberate consequence of reusing the renderer, not an oversight.
- One defect can produce two lines: condition:null yields BOTH a structure error ('required field missing from edge #0: "condition"') and the edge_with_condition violation. That is renderReport's existing structure-then-soundness order, unchanged here — visible now because this is the first surface that renders a report for a document nobody tried to write.
- Files touched beyond the ticket's declared shared-file surface: docs/spec/screen.md only. Its §1 row still claimed the fragment answers {chat, map, done}, and §4 counts the core routes this layer asked for — a count pinned by spec-routes.test.ts AT3, so adding the row required 'five' -> 'six' in two places. No test file outside the declared list was edited.
- packages/core/test/graph-validate-routes.test.ts's AT5 rebuilds apply's resulting document with applyOperations(document, operations) rather than reading it back: apply never publishes the document it judged, and comparing against the registered snapshot alone would compare the wrong document.
