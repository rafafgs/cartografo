### t464 (developing, unverified)

- FR1 and FR5 are mutually exclusive as written, and the bundle validator does NOT catch it — `cartografo import` does. `domain/graph.ts`'s `producedPaths` counts only a pinned skill's REQUIRED output properties as guaranteed ('an optional one is a possibility, and a possibility is not something the node downstream can be held to'), so making `skills` optional on interview's output while requiring `interview.skills` on deliver's input produces `unproduced_input: node "deliver" requires the input path "interview.skills", which nothing supplies`. The bundle then validates locally but cannot be registered, which shows up as 27 screen tests failing on 'the control plane imports the interview bundle at startup'. Resolution taken: `skills` declared but not required on deliver's bucket — which FR9 already implies ('may legitimately hold {graph} with no skills key at all') and which deliver's own hand checks + `checked.problems` already cover.
- `deliver-bundle.json`'s instructions never actually contained the literal `input.interview.draft` FR6 names — only the precondition and the prose 'What you have is a draft'. Both were rewritten to name `input.interview.graph` / `input.interview.skills` explicitly, so the write-out section now says which key each file comes from.
- The two manifests round-trip exactly through `json.dumps(..., indent=2, ensure_ascii=False) + '\n'`, so they can be edited programmatically with no formatting churn — but watch the apostrophes: a naive rewrite of a description silently introduced a U+2019 into otherwise-ASCII prose.
