### t465 (developing, unverified)

- lastAttemptAt is stamped BEFORE the call, never after: stamping on completion lets a control plane slower than one interval turn the next line into a second in-flight draft.
- DoD without a browser: packages/runner/test/fixtures/fake-engine.mjs's FAKE_ENGINE_HEARTBEAT_MS is the seam that spaces onOutput past the throttle; driving factory-graph-map-design.e2e.test.ts's boot shape plus startScreenRouter is enough to watch the fragment change. A human visual pass over /interview/:id is still owed, same caveat t458 recorded.
