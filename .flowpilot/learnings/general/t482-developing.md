### t482 (developing, unverified)

- I also updated the `interview` node's two agentic check paraphrases in graph.json, which the Code Changes table did not ask for — the node's contract.checks are a second copy of the manifest's checks and would otherwise still say 'asking exactly one thing' and 'compare the turn's question'. Safe by AT3c of validate-factory-bundle ('the prose of an agentic item is free to differ from the check'); only counts, order and deterministic commands are compared.
- The hash has to be recomputed AFTER the last prose edit, not once: I rewrapped one over-long line in the instructions after the first pin and had to redo both files. The pin script is `manifestHash(JSON.parse(interview.json))` — it hashes only {instructions, input, output, checks, permissions, budgets, command}, so `version` and `description` changes alone would NOT move it.
- In the rescripted e2e, `dispatched` has only two entries at the point where the t440 skill_source carry-over is asserted (the delivering dispatch has not run yet). The old `for (const turn of [1, 2])` loop silently depended on there being three asking turns; with two it reads `dispatched[2]` before it exists, so it is now a single direct assertion on `dispatched[1]`.
- `DRAFTS[2]` (triage settled, resolve not yet) is no longer reported by any turn of the t360 crossing — the form settles triage and the next turn is the final one. It is still load-bearing for the t464 AT7 test, which asserts `deliver` is dispatched with the manifests an EARLIER turn reported, so do not delete it as dead.
