### t491 (developing, unverified)

- `GET /v1/runners` filtering to present runners silently invalidates any existing test that checks pairing AFTER stopping a runner. t186 AT2 was the only one in the tree, but the pattern (`startRunner` -> `stop()` -> read the fleet) is common in run.e2e.test.ts and the next test written that way will pass for the wrong reason.
- The runner CLI already prints `runnerId` on its readiness line (`cartografo.runner.ready`), which is the seam for any test that needs the identity a spawned runner derived. With `spawnWatched` + `awaitReadiness` from @cartografo/test-support, spawning the real binary costs about five lines — worth knowing before anyone reaches for the in-process `startRunner`, which has no signal handlers and derives its identity from the TEST process's cwd.
- packages/runner/test/engine/discover-mcp-servers.claude-code.test.ts's 't400 — the deadline reaches the fallback too' is FLAKY on this machine, independently of this branch: it fails with `ENOENT ... /pid` (the fixture reads a pid file the killed helper had not written yet). Observed passing 4 of 6 isolated runs; nothing under src/engine or test/engine is in this branch's diff. The full `npm test` was green.
