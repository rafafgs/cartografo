### t492 (developing, unverified)

- The `npm test` red is NOT this branch. `git diff main -- packages/runner` is empty, and the same tree gave 906/906 pass on one isolated `npm test` in packages/runner and 6 failures on the very next. The failures are always the same wall-clock family — conformance-kit C3 (a 1s timeout whose fake engine must write record.json within 5s), C9 (`stopped after 0 of 4` heartbeats), and t400's deadline test — i.e. starvation on a machine running several worktree agents at once. Judge this branch by core+screen+root, all green twice.
- `unproduced_input` (`domain/graph.ts`) also reads the PINNED SKILL's `output.properties`, not the node's contract. So changing the fixture's first skill output to `{done, draft}` forced a second edit: the step below it had to drop its claim on a `note`, or the class becomes unregistrable. Two edits, and the second is not optional.
- `fillSkillRefs` from `packages/screen/src/register-map.ts` is the way to register a fixture graph from a screen test — screen deliberately imports nothing from packages/core, and register-map carries its own transcription of the manifest hash recipe.
- AT3 and AT5 pass before the implementation and that is the honest state, not a broken red: they pin the top-level precedence and the malformed-`draft` robustness, both of which the old code already had. The real red is AT1, AT2, AT4's second half, AT6 and AT7.
- The fix also changes behaviour for a report that carries a top-level `draft` object alongside nothing else — it is now read as a map wrapper. No registered contract can produce one (interview.json's `additionalProperties: false` refuses `draft` outright), but a hand-written test fixture using `draft` as an unrelated field name would now be interpreted.
