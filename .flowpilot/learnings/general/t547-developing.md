### t547 (developing, unverified)

- cartografo_list_runners' digest deliberately omits the screen's per-engine HTML 'fix' prose (install commands, mcp add text) per the ticket's own Out of Scope — only field/met/headline travel; a model acts via cartografo_get_settings/update_settings/request_runner_recheck instead.
- The 'decide it at the screen' phrasing also appeared in cartografo_list_proposals's own tool description, not just in tools.ts's header comment and protocol.ts's INSTRUCTIONS — fixed all three plus both READMEs so no stale pointer to the screen survives for a proposal decision.
