### t548 (developing, unverified)

- API_SUBCOMMANDS in packages/core/src/cli/index.ts is not exported, and this ticket may not change src/cli. So cli-spec-routes.test.ts reads the array literal from the source as text, the same way spec-routes.test.ts reads router.ts. If the array ever stops being a flat `const API_SUBCOMMANDS = [ '...' ];` literal, that test fails with a message saying the pin has to follow it.
- Red confirmed before any doc existed: both tests failed on 'spec does not exist', not on an import or fixture error. Removing one row also made AT1 fail with 'subcommands with no row in cli.md §1: examples'. Rows that differ only in verb (`job <id>` / `job create`) are one subcommand in the set.
- Walkthrough (the ticket's TDD exception), run on fresh CARTOGRAFO_DB_PATH, HOME=/tmp and port 4617 with --no-runner. That flag was deliberate, so no real claude-code sessions were dispatched on the founder's account; a finished traversal was therefore NOT reached. These all worked with exit 0: up --no-screen --no-browser, runners (prints the pairing command), import software-development, status, examples, job create --graph <id> --input job.json, jobs, job 1, input-requests, example run asymmetric-bets, and proposals list. block/unblock showed up live in `watch --job 1 --until-done` (killed by hand, since the job could not finish), and `watch --job 1 --from-start` replayed all three events. The finished-traversal step is still owed.
- `jobs` prints a `blocked` true/false column but not the reason; only `job <id>` prints `blocked — <reason>`. getting-started §5 was written to match.
- --by defaults differ by subcommand: answer, block, unblock and interview fall back to the literal "operator", while proposals and graph propose refuse with a usage error when no OS user can be resolved. cli.md §4 states both; index.ts's USAGE only hints at it.
- The successor line was added directly under each screen spec's metadata block, with no blank line, so it renders in the same paragraph as the other metadata lines, like the existing ones do. spec-routes.test.ts, inbox-spec-routes.test.ts, graph-editor-acceptance.test.ts and interview.test.ts all pass unmodified in the full run.
- Full npm test: core 990/990, runner 909 pass / 0 fail of 911 (the same 2 not counted that t584/t545 recorded), and all of mcp, screen, the other workspaces and root green on the first run, with no flake this time.
