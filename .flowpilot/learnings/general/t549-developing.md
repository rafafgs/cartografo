### t549 (developing, unverified)

- Never write the screen's old command name literally in any tracked file outside DECISIONS.md, CHANGELOG.md or notes/, and that includes .flowpilot/learnings. The new check-screen-retired lint gate fails on it. Its own script and test build the name with ['cartografo','screen'].join('-') for exactly this reason.
- npm install after deleting a workspace directory does NOT drop its `packages/<name>` node from package-lock.json; it marks it `extraneous: true`, and `npm prune` leaves it too. I removed that one node with a JSON rewrite, then confirmed a second `npm install` left the lockfile byte-identical.
- The red run of cli-up-unit's FR2 case against the OLD up.ts does not fail fast. `runCli(['--no-screen'])` really starts a control plane (and a runner, with the real HOME) and hangs until the test times out. It also wrote packages/core/.cartografo/cartografo.db (gitignored), which I deleted.
- AT9 run from an empty tmp dir with HOME=tmp and port 4719: `up` spawned exactly one child (node .../bin/cartografo-runner.mjs) and no open/xdg-open, stopped cleanly on SIGINT, and `--no-screen` exits 2 with 'up does not understand'. Only --help points at cli.md/mcp-server.md; the readiness stdout is unchanged, as FR1 requires.
- The image logs 'could not import the map-design bundle from /usr/local/lib/factory-graphs/map-design — path not found' at startup. factory-graphs/ is not in the package's files, so this predates this ticket, but the interview class is never registered in a container.
- In cli.md/mcp-server.md the known-gap entry linked ../../packages/screen/src/mcp-catalog.ts, a sixty-first dead link FR11 did not name. I turned it into a backticked path, not a deletion, so the future MCP-suggestions ticket can still find the file in git history.
- Full npm test on the final tree: core 994/994 (including pack-install e2e and docker-image e2e, which ran because Docker is available), runner 911 tests with 909 pass, 2 skipped, 0 fail, mcp 60, surveyor 26, cost-surveyor 34, root 330, no flakes. startup.test.ts alone: 15/15.
