# Dependencies
node_modules/

# Embedded database of the control plane (D1) — the default directory of CARTOGRAFO_DB_PATH
.cartografo/
*.db
*.db-shm
*.db-wal

# Build artefacts / tool caches
dist/
*.tsbuildinfo
.eslintcache

# Project profile the ticket tool assembles by itself on every refinement, out
# of DECISIONS.md/README.md/notes — a cache, not a source of truth: versioning it
# would only guarantee a portrait of the repo that goes stale on every change of
# its shape.
.flowpilot/

# System
.DS_Store
