# Dependências
node_modules/

# Banco embarcado do control plane (D1) — o diretório default de CARTOGRAFO_DB_PATH
.cartografo/
*.db
*.db-shm
*.db-wal

# Artefatos de build / cache de ferramenta
dist/
*.tsbuildinfo
.eslintcache

# Perfil do projeto que a ferramenta de fichas monta sozinha a cada refino, a
# partir de DECISIONS.md/README.md/notas — cache, não fonte de verdade: versionar
# só garantiria um retrato do repo desatualizado a cada mudança de forma dele.
.flowpilot/

# Sistema
.DS_Store
