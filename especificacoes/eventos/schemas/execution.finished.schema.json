{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "execution.finished.schema.json",
  "title": "execution.finished",
  "description": "The execution is over: every job of it reached a final node and no active lease holds any of them any more. A fact only the control plane asserts (D1, D21), and the one an unprivileged observer starts from to run the lenses. No payload: envelope.execution_id, entity.id and occurred_at already say which round ended and when.",
  "type": "object",
  "allOf": [
    {
      "$ref": "envelope.schema.json"
    }
  ],
  "properties": {
    "type": {
      "const": "execution.finished"
    },
    "entity": {
      "type": "object",
      "properties": {
        "type": {
          "const": "execution"
        }
      }
    },
    "data": {
      "type": "object",
      "required": [],
      "properties": {},
      "additionalProperties": false
    }
  }
}
