{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "input_request.created.schema.json",
  "title": "input_request.created",
  "description": "An agent needs something from the human before continuing. Parity with flowpilot's input_requests + EventKind.INPUT_REQUESTED: a question and an approval are the same animal, and the kind field is the only difference.",
  "type": "object",
  "allOf": [
    {
      "$ref": "envelope.schema.json"
    }
  ],
  "properties": {
    "type": {
      "const": "input_request.created"
    },
    "entity": {
      "type": "object",
      "properties": {
        "type": {
          "const": "input_request"
        }
      }
    },
    "data": {
      "type": "object",
      "required": [
        "job_id",
        "kind",
        "question",
        "auto_approvable"
      ],
      "properties": {
        "job_id": {
          "type": "integer",
          "description": "The job left waiting for the answer."
        },
        "session_id": {
          "type": [
            "integer",
            "null"
          ],
          "description": "The session that asked. Optional because the path of the answer outlives the session: it emits the request and the process ends."
        },
        "node_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "The node the job was on when it asked (t167). Stamped by the server from the owning job, never coming from the request body — the same trust boundary as job_id. NULL means \"it is not known which node it came from\": a job with no position, or a request recorded before the field existed."
        },
        "kind": {
          "enum": [
            "question",
            "approval"
          ],
          "description": "question: somebody needs to know something. approval: a manual gate wants an OK on an artefact."
        },
        "question": {
          "type": "string",
          "description": "What is being asked."
        },
        "context": {
          "type": [
            "string",
            "null"
          ],
          "description": "Evidence, the alternatives weighed, what has already been tried."
        },
        "options": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          },
          "description": "Short labels for the possible decisions."
        },
        "recommendation": {
          "type": [
            "string",
            "null"
          ],
          "description": "What the agent would choose, in the imperative."
        },
        "default_answer": {
          "type": [
            "string",
            "null"
          ],
          "description": "The value auto-approval applies on the human's behalf."
        },
        "auto_approvable": {
          "type": "boolean",
          "description": "Whether the auto-approval gate MAY answer in the human's place. Carried explicitly: \"no options and no default\" was never a good proxy for unapprovable."
        }
      },
      "additionalProperties": false
    }
  }
}
