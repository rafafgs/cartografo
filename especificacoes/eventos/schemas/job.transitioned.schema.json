{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "job.transitioned.schema.json",
  "title": "job.transitioned",
  "description": "O trabalho andou de um nó para outro. Divergência justificada do EventKind.STATE_CHANGE do flowpilot: nós do grafo congelado por versão, não os 12 estados fixos de um TicketState.",
  "type": "object",
  "allOf": [
    {
      "$ref": "envelope.schema.json"
    }
  ],
  "properties": {
    "type": {
      "const": "job.transitioned"
    },
    "entity": {
      "type": "object",
      "properties": {
        "type": {
          "const": "job"
        }
      }
    },
    "data": {
      "type": "object",
      "required": [
        "to_node_id"
      ],
      "properties": {
        "from_node_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "Nó de origem; null na primeira transição, quando o trabalho sai do nó de entrada pela primeira vez."
        },
        "to_node_id": {
          "type": "string",
          "description": "Nó de destino, dentro da versão de grafo congelada para esta execução."
        }
      },
      "additionalProperties": false
    }
  }
}
