{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "lease.expired.schema.json",
  "title": "lease.expired",
  "description": "The possession lapsed and the job goes back to the queue (D5). A cartografo extension, with no flowpilot equivalent.",
  "type": "object",
  "allOf": [
    {
      "$ref": "envelope.schema.json"
    }
  ],
  "properties": {
    "type": {
      "const": "lease.expired"
    },
    "entity": {
      "type": "object",
      "properties": {
        "type": {
          "const": "lease"
        }
      }
    },
    "data": {
      "type": "object",
      "required": [
        "runner_id",
        "reason"
      ],
      "properties": {
        "runner_id": {
          "type": "string",
          "description": "The runner that lost the possession."
        },
        "reason": {
          "enum": [
            "heartbeat_lost",
            "ttl_elapsed"
          ],
          "description": "heartbeat_lost: the runner stopped signalling before the deadline. ttl_elapsed: the deadline ran out with no renewal."
        }
      },
      "additionalProperties": false
    }
  }
}
