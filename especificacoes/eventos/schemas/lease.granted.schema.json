{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "lease.granted.schema.json",
  "title": "lease.granted",
  "description": "A runner took possession of a job for a deadline (D5). A cartografo extension: flowpilot has a single runner and has no equivalent.",
  "type": "object",
  "allOf": [
    {
      "$ref": "envelope.schema.json"
    }
  ],
  "properties": {
    "type": {
      "const": "lease.granted"
    },
    "entity": {
      "type": "object",
      "properties": {
        "type": {
          "const": "lease"
        }
      }
    },
    "data": {
      "type": "object",
      "required": [
        "job_id",
        "runner_id",
        "expires_at"
      ],
      "properties": {
        "job_id": {
          "type": "integer",
          "description": "The job dispatched under this possession."
        },
        "runner_id": {
          "type": "string",
          "description": "The runner that took the possession."
        },
        "expires_at": {
          "type": "string",
          "format": "date-time",
          "description": "When the possession lapses if the heartbeat does not renew it."
        }
      },
      "additionalProperties": false
    }
  }
}
