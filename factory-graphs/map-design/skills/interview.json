{
  "id": "interview",
  "version": "1.2.0",
  "hash": "sha256:e28ff0fc65b8841715bead7a7d51fe8650ba2894200d7391a130fc0aace65f76",
  "role": "work",
  "description": "Interviews a person about a problem they keep solving, one question per session, and reports the map under construction after every answer.",
  "input": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "required": [
      "job"
    ],
    "properties": {
      "job": {
        "type": "object",
        "description": "the request as the person wrote it, published at `input.job` (t253)",
        "required": [
          "id",
          "title",
          "body"
        ],
        "properties": {
          "id": {
            "type": "integer"
          },
          "title": {
            "type": "string"
          },
          "body": {
            "type": [
              "string",
              "null"
            ],
            "description": "the problem in the person's own words; `null` when they gave only a title"
          }
        }
      },
      "perguntas_respondidas": {
        "type": "array",
        "description": "every question of this interview that already came back, in the order they were answered",
        "items": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string"
            },
            "pergunta": {
              "type": "string"
            },
            "resposta": {
              "type": "string"
            }
          }
        }
      },
      "interview": {
        "type": "object",
        "description": "this node's own bucket: the map as the turns before this one left it, key by key — `graph` from the last turn that reported one, `skills` from the last turn that settled a manifest",
        "properties": {
          "done": {
            "type": "boolean"
          },
          "graph": {
            "type": "object",
            "description": "the graph document as the last turn left it; absent on the first dispatch of a fresh interview, which has settled nothing yet"
          },
          "skills": {
            "type": "array",
            "description": "every manifest settled so far, as the last turn that reported any left them; it survives a turn that does not repeat it, because the bucket merge is per key",
            "items": {
              "type": "object"
            }
          },
          "skill_source": {
            "type": "object",
            "description": "the folder or repository of existing skills this person named, as an earlier turn reported it; it survives without being repeated, because the bucket merge is per key",
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "path",
                  "git"
                ]
              },
              "location": {
                "type": "string"
              }
            }
          }
        }
      },
      "environment": {
        "type": "object",
        "description": "what only the machine running the session knows (t360, t440): which MCP servers this engine can reach, which registered classes read like this request, and the drafts derived from the skills this person already has",
        "properties": {
          "mcp_servers": {
            "type": [
              "array",
              "null"
            ],
            "items": {
              "type": "string"
            },
            "description": "the servers this engine names; `null` means this engine implements no discovery at all, which is NOT the same as finding none"
          },
          "similar_classes": {
            "type": "array",
            "description": "registered classes whose current version reads like this request, best first",
            "items": {
              "type": "object",
              "properties": {
                "class": {
                  "type": "string"
                },
                "name": {
                  "type": "string"
                },
                "description": {
                  "type": "string"
                },
                "score": {
                  "type": "number"
                }
              }
            }
          },
          "skill_drafts": {
            "type": "array",
            "description": "one draft manifest per `SKILL.md` found at the source this person named, derived for you to adapt; empty when they named none. Nothing in it was executed, and nothing in it is registered",
            "items": {
              "type": "object"
            }
          },
          "skill_drafts_error": {
            "type": [
              "string",
              "null"
            ],
            "description": "why that source could not be read — a missing folder, a clone this workspace does not allow, a repository nobody could reach. Relay it to the person and move on; nothing retries it"
          }
        }
      }
    }
  },
  "output": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "required": [
      "done",
      "graph"
    ],
    "additionalProperties": false,
    "properties": {
      "done": {
        "type": "boolean",
        "description": "false while there is still something to ask; true on the last turn, which asks nothing"
      },
      "graph": {
        "type": "object",
        "description": "the graph document under construction, in the format of `schema/graph.schema.json`; partial while `done` is false, and reported WHOLE every turn — never a patch"
      },
      "skills": {
        "type": "array",
        "description": "one skill manifest per node, in the format of `specs/formats/skill-manifest.schema.json`, WITHOUT `hash` — the pin is computed by whoever registers. Reported only on a turn that created or edited one, and then as the WHOLE cumulative array: the merge is per key and never per array item, so a shorter array deletes every manifest missing from it",
        "items": {
          "type": "object"
        }
      },
      "skill_source": {
        "type": "object",
        "description": "where this person's existing skills are, reported ONCE by the turn that asked for them; a later turn that omits it does not lose it, because the report merge is shallow and per key",
        "required": [
          "kind",
          "location"
        ],
        "properties": {
          "kind": {
            "type": "string",
            "enum": [
              "path",
              "git"
            ],
            "description": "`path` for a folder on this machine, `git` for a repository URL"
          },
          "location": {
            "type": "string",
            "description": "the path or the URL, as the person said it"
          }
        }
      }
    }
  },
  "preconditions": [
    "a person is reachable: this node asks, and a map is never finished by guessing",
    "the request carries the problem in the person's own words, or a first question is spent getting it"
  ],
  "checks": [
    {
      "id": "one-question-and-a-whole-draft",
      "type": "agentic",
      "description": "The three ways this node fails silently: asking two things at once, which makes an answer unattributable; reporting a graph that lost what an earlier turn had already settled; and reporting a `skills` array shorter than the one an earlier turn reported, which deletes every manifest missing from it.",
      "instruction": "Read the session's own output and answer all three, quoting the exact text that supports each answer. 1. Does the turn end with AT MOST ONE `input-request` block, and does that block ask exactly one thing — no compound question, no list of decisions? 2. Does the reported `graph` still carry everything the previous turns settled, plus what this turn's answer added? Name any node, edge or field that was present before and is missing now. 3. ONLY IF this turn reported `skills` at all: is that array the WHOLE set of manifests settled so far, and not just the one this turn changed? A turn that omits `skills` entirely is correct and must NOT be failed for it — the bucket merge keeps what the last turn that reported it set — but a turn that reports FEWER manifests than an earlier turn did has silently deleted the rest, because the merge is per key and never per array item.",
      "required_evidence": [
        "the fenced block as printed",
        "the previous turn's report, for the comparison"
      ]
    },
    {
      "id": "mcp-suggestion-hint-when-unmatched",
      "type": "agentic",
      "description": "The hint is a promise to whoever reads the turn: a line that is missing costs the person the three suggestions they needed, and a line written for a step the machine already covers sends them shopping for a server they have.",
      "instruction": "Read this turn's own `input-request` block and the `environment.mcp_servers` list the session was dispatched with, and answer both, quoting the exact text that supports each answer. 1. Is this question about a step reaching an external system that NONE of the discovered servers covers — a `null` list covers nothing — and if so, does `context` carry a line of its own reading exactly `NEEDS_MCP_SERVER: <phrase>`? 2. If that line IS present, is the phrase a capability rather than a product or vendor name, and is it true that no listed server would have served the step?",
      "required_evidence": [
        "the `context` field as printed",
        "the `environment.mcp_servers` list this turn was dispatched with"
      ]
    },
    {
      "id": "skill-draft-adaptation-not-a-shortcut",
      "type": "agentic",
      "description": "A draft derived from somebody else's `SKILL.md` is a starting point, not an answer. Copied through unchanged it produces a step nobody was asked about — and a permission set nobody approved.",
      "instruction": "Read the `environment.skill_drafts` this turn was dispatched with and the skill manifests in the draft it reported, and answer both, quoting the exact text that supports each answer. 1. For every reported manifest that clearly started from one of those drafts, do its `input`, `output` and `checks` reflect an answer somebody gave in this interview — rather than the draft's own placeholders (`revisor humano escreve o JSON Schema aqui`) or its derived commands, carried over verbatim? Name any field that is still the draft's. 2. Are that manifest`s `permissions` at or under the safe default the draft arrived with — `filesystem.read` only, `filesystem.write` empty, `network.allowed` false? Quote them.",
      "required_evidence": [
        "the `environment.skill_drafts` this turn was dispatched with",
        "the reported manifest, as printed"
      ]
    }
  ],
  "permissions": {
    "filesystem": {
      "read": [
        "**"
      ],
      "write": [
        "**"
      ]
    },
    "network": {
      "allowed": false
    }
  },
  "instructions": "# Interview one person into a map\n\nSomebody keeps solving the same kind of problem by hand and wants it to become a\nmap: a graph of steps, each with a contract, that this product can then run.\nYour job is to ask them about it — **one question per session** — and to hand\nback the map as it stands after every answer.\n\n**The request:** {{input.job.title}} (#{{input.job.id}})\n\n{{input.job.body}}\n\n## How a turn works, and why it is shaped like this\n\nThere is no session resume in this product. **Resuming is re-dispatching**: when\nyou end your turn with a question, the control plane blocks the work, a person\nanswers, and you are started again as a fresh session with the whole exchange\nalready written into your prompt, under `## What you already asked, and what\ncame back`. So:\n\n- **Continue from the last answered question. Never ask again what was already\n  answered** — the answers above are decided, and re-asking spends a turn and\n  the person's patience for nothing.\n- **Ask exactly one thing per turn.** Two questions in one block come back as one\n  answer, and there is no way to tell which half it answers.\n- **Report the graph every turn, whole.** It is the only record of what has been\n  settled: a `graph` you do not report this turn is gone next turn. `skills`\n  follows a different rule, and the section below is where it is written down.\n- **Twenty questions is the ceiling.** If the map is not finished by then, close\n  it with what you have and say in the graph's own metadata which steps are still\n  rough. A person who has answered twenty questions has given you enough.\n\n## What to ask, and in what order\n\n1. **The class.** What do they call this kind of problem? A short kebab-case name\n   — it becomes the class id. Ask it first, unless the request already names one.\n   If `environment.similar_classes` scores an existing class, **suggest it as the\n   recommendation** — the person decides, never you: an existing class may be the\n   same problem under another name, or a different problem that reads alike.\n2. **What they already have.** Ask, once and right here — before you start\n   walking the steps — whether they already have skills or prompts they use for\n   this kind of work, and if so where: a folder on this machine, or a git URL.\n   A \"no\" ends the subject: report no `skill_source`, and nothing else about the\n   interview changes.\n\n   When they name one, report it beside your map, on that turn:\n\n   ```resultado\n   {\"done\": false, \"graph\": {…}, \"skill_source\": {\"kind\": \"path\", \"location\": \"/Users/somebody/skills\"}}\n   ```\n\n   `kind` is `path` for a folder and `git` for a URL, and `location` is what they\n   said, unchanged. Report it ONCE — it is kept for you, and no later turn has to\n   repeat it, unlike the graph. From the next turn on, `environment.skill_drafts`\n   carries one draft manifest per `SKILL.md` found there, derived for you to read.\n   Nothing at that source is ever executed, and nothing from it is registered: a\n   draft becomes a skill only when a person registers the bundle.\n\n   If `environment.skill_drafts_error` is not null, put that message, verbatim, in\n   your next question's `context`, and carry on with the interview. Nothing\n   retries it: a source that did not read costs them the head start and nothing\n   else.\n\n3. **The steps.** Walk the work as they describe it, one step at a time, and for\n   each step ask, in this order and one question per turn:\n   - **what it needs** before it can start — this becomes the step's\n     `contract.input_schema`;\n   - **what it produces**, and — when the step has more than one way out — the\n     labels those exits carry. Both go into `contract.output_schema`, and the\n     labels are the `condition` of the edges leaving it;\n   - **how you know it went well** — this becomes `contract.checks`;\n   - **what usually goes wrong there**. This is the most valuable question in the\n     interview: whatever they answer becomes a check of its own, because a step's\n     real checks are its real failures written down;\n   - **whether it reaches outside** the machine — an API, a database, a browser —\n     and **through which server**. `environment.mcp_servers` is what this engine\n     can currently reach: offer those names, and record the answer in the step's\n     own description when it names one. A `null` there means this engine reports\n     no discovery at all — say so, and ask them plainly.\n\n     If **none** of those names covers what they just described — including when\n     `environment.mcp_servers` is `null`, because \"this engine cannot answer\" is\n     not \"there is nothing\" — then, beside your ordinary prose, put ONE line by\n     itself inside that question's `context`, spelled exactly like this:\n     `NEEDS_MCP_SERVER: <a short capability phrase>`. Name the **capability** and\n     never a product — `calendar`, `spreadsheet`, `issue tracker`, not the brand\n     of the one you happen to know. The screen reads that line, looks the phrase\n     up in the public MCP registry and shows the person up to three servers with\n     the command that would add each one; a brand name narrows that search to a\n     decision nobody asked you to make. Write it at most once per turn, only when\n     nothing on the list fits, and never instead of explaining yourself: the line\n     is one extra line inside `context`, and the rest of `context` stays exactly\n     as useful as it would have been without it. Whatever they answer, record it\n     in the step's own description as usual — nothing about this line changes the\n     map, and nothing installs anything.\n4. **The ends.** Which step starts the work, and which ones finish it.\n\n### When they already had a skill for the step\n\nWhen `environment.skill_drafts` carries anything, start each step's manifest from\nthe draft closest to it — by name and description, your own judgement, and say\nwhich one you took — instead of from nothing. **Adapting is not copying.** Ask\nevery contract question above anyway: what it needs, what it produces, how you\nknow it went well, what usually goes wrong there, and which server it reaches\nthrough. Their answers are what replace the draft's placeholders, and a manifest\nstill carrying `revisor humano escreve o JSON Schema aqui`, or the draft's\nderived commands as the whole of its `checks`, is a step nobody was asked about.\n\nAnd never widen a draft's `permissions`. They arrive read-only and offline —\n`{\"filesystem\": {\"read\": [\"**\"], \"write\": []}, \"network\": {\"allowed\": false}}` —\nbecause that is what an imported skill is born with, and widening any of the\nthree is a decision a person makes at the registry gate, never one you make\ninside a draft.\n\n## Every question carries a value they can accept in one click\n\nEnd your turn with exactly ONE fenced block, and nothing after it:\n\n```input-request\n{\"question\": \"<one question, in their language and not in this product's>\",\n \"context\": \"<what you already know, and why this is the next thing to settle>\",\n \"options\": [\"<a real choice>\", \"<another one>\"],\n \"recommendation\": \"<the value you would take; it is the one-click answer>\",\n \"default\": \"<the option that applies if they simply accept>\"}\n```\n\n`recommendation` is never optional: a person reading this should be able to press\naccept and be right most of the time. `options` are for when the choices really\nare a short list — leave them out rather than inventing three.\n\nAnother answer is always possible. Nothing you offer is binding on them, and an\nanswer that fits none of your options is the most valuable one you can get:\nfollow it rather than steering them back.\n\n## Report the map, every turn\n\nBefore or with the question, print the map as it stands:\n\n```resultado\n{\"done\": false, \"graph\": {…}, \"skills\": [{…}]}\n```\n\n`graph` and `skills` are two INDEPENDENT keys of your report, and the rule is not\nthe same for the two:\n\n- **`graph`, every turn, whole** — never a patch. What a turn leaves out of it is\n  gone at the next turn.\n- **`skills` only on the turn that created or edited a manifest** — and then the\n  WHOLE cumulative array: every manifest settled so far, never only the one that\n  changed. A turn that omits `skills` keeps what the last turn that reported it\n  set, exactly the way `skill_source` is kept. A turn that reports a SHORTER\n  array deletes every manifest missing from it, silently — the merge is per key\n  and never per array item. Report it once more on the final `done: true` turn\n  if anything is still unreported.\n\nAnd what each of the two has to contain:\n\n- `graph` is a graph document (`schema/graph.schema.json`): `problem_class`,\n  `lineage`, `metadata`, `nodes`, `edges`, `initial_node`, `final_nodes`,\n  `custom_fields`. It is partial while the interview runs, and complete on the\n  last turn.\n- Every node carries `id`, `role`, `node_type` (`work` or `gate`), `description`,\n  a `skill_ref` naming the manifest below it, and a `contract` with\n  `input_schema`, `output_schema` and **`checks` with at least one item**. A\n  check is either deterministic — `{\"type\": \"deterministic\", \"command\": \"…\"}` —\n  or agentic: `{\"type\": \"agentic\", \"instruction\": \"…\", \"required_evidence\":\n  true}`. Prefer a command wherever the answer to \"how do you know it went well\"\n  was something a machine could run.\n- `skills` carries ONE manifest per node, in the skill-manifest format:\n  `id`, `version`, `role`, `description`, `input`, `output`, `preconditions`,\n  `checks`, `permissions`, `instructions`, `origin`. **Write no `hash`**: the pin\n  is computed by whoever registers the bundle, and a hash you invent is a pin\n  that will not close. The `checks` of a manifest must line up with the\n  `contract.checks` of its node — same items, same order, same commands.\n- Each node's `skill_ref` names the manifest's `id` and `version` and nothing\n  else, for the same reason.\n\n## When the map is finished\n\nOn the last turn, ask nothing at all: print no `input-request` block, and report\n\n```resultado\n{\"done\": true, \"graph\": {…}, \"skills\": [{…}]}\n```\n\nA complete map has every node's three contract fields filled, one manifest per\nnode, an `initial_node`, at least one final node, and every node reachable from\nthe first and able to reach a last one. Nobody registers it here — the step after\nyou checks its shape, and a person decides whether it becomes a map.\n",
  "origin": {
    "type": "native"
  }
}
