{
  "id": "draft-note",
  "version": "1.0.0",
  "hash": "sha256:572adcb6f5c03f6c571c07441702b4a89e9976fb2280f44701dab50a5fb20690",
  "role": "work",
  "description": "Writes a short note in `nota.md`, and nothing beyond that.",
  "input": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "required": [
      "tema"
    ],
    "properties": {
      "tema": {
        "type": "string",
        "minLength": 1
      }
    }
  },
  "output": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "required": [
      "texto"
    ],
    "properties": {
      "texto": {
        "type": "string",
        "minLength": 1
      }
    }
  },
  "preconditions": [
    "the session directory exists and is where `nota.md` is going to be written"
  ],
  "checks": [
    {
      "id": "note-exists",
      "type": "deterministic",
      "description": "The note exists and is not empty.",
      "command": "test -s nota.md",
      "timeout_s": 60
    }
  ],
  "permissions": {
    "filesystem": {
      "read": [
        "**"
      ],
      "write": [
        "**"
      ]
    },
    "network": {
      "allowed": false
    }
  },
  "instructions": "# Draft a short note\n\nWrite the file `nota.md` in the current directory, with 3 to 5 lines about the\ntopic the work asks for.\n\nDo not create any other file. Do not commit, do not create a branch, do not run git.",
  "origin": {
    "type": "native"
  }
}
