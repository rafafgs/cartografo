{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "input_request.auto_resolved.schema.json",
  "title": "input_request.auto_resolved",
  "description": "The auto-approval gate answered on the human's behalf. Parity with flowpilot's AnswerSource.AUTO: the audit ALWAYS distinguishes approved-by-user from approved-by-system — here the distinction is the type of the event.",
  "type": "object",
  "allOf": [
    {
      "$ref": "envelope.schema.json"
    }
  ],
  "properties": {
    "type": {
      "const": "input_request.auto_resolved"
    },
    "entity": {
      "type": "object",
      "properties": {
        "type": {
          "const": "input_request"
        }
      }
    },
    "data": {
      "type": "object",
      "required": [
        "answer",
        "based_on"
      ],
      "properties": {
        "answer": {
          "type": "string",
          "description": "What was applied on the human's behalf."
        },
        "based_on": {
          "enum": [
            "recommendation",
            "default_answer",
            "precedent"
          ],
          "description": "Where the answer came from: the agent's own recommendation, the default answer declared in the request, or a precedent recorded in the log."
        }
      },
      "additionalProperties": false
    }
  }
}
