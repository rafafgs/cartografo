{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "job.amended.schema.json",
  "title": "job.amended",
  "description": "The content of the job was edited. Parity with flowpilot's EventKind.AMENDED, including in its discipline: an audit record, not a version history.",
  "type": "object",
  "allOf": [
    {
      "$ref": "envelope.schema.json"
    }
  ],
  "properties": {
    "type": {
      "const": "job.amended"
    },
    "entity": {
      "type": "object",
      "properties": {
        "type": {
          "const": "job"
        }
      }
    },
    "data": {
      "type": "object",
      "required": [
        "changed_fields"
      ],
      "properties": {
        "changed_fields": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "type": "string"
          },
          "description": "Only the NAMES of the fields that changed, never the content. Whoever wants the new text reads the job; the log says what was touched and by whom."
        }
      },
      "additionalProperties": false
    }
  }
}
