{
  "id": "gate-without-evidence",
  "version": "1.0.0",
  "hash": "sha256:621439431f915c412bb3d251df396d475791172eae95ccd9952ca0ec6e40486c",
  "role": "gate",
  "description": "NEGATIVE FIXTURE — this is not an example of a valid manifest. It exists to prove that the schema refuses an agentic check with no required evidence.",
  "input": {
    "type": "object",
    "required": [
      "artifact"
    ],
    "properties": {
      "artifact": {
        "type": "string"
      }
    }
  },
  "output": {
    "type": "object",
    "required": [
      "outcome"
    ],
    "properties": {
      "outcome": {
        "type": "string",
        "enum": [
          "pass",
          "fail",
          "escalate_human"
        ]
      }
    }
  },
  "preconditions": [
    "artifact produced by the previous node"
  ],
  "checks": [
    {
      "id": "criteria-met",
      "type": "agentic",
      "description": "Checks whether the acceptance criteria were met — and this is exactly where the manifest breaks: with no required_evidence, the gate could conclude from the self-report of whoever produced the artifact, which is what D9 and the learning note forbid.",
      "instruction": "Check whether the acceptance criteria were met."
    }
  ],
  "permissions": {
    "filesystem": {
      "read": [
        "**"
      ],
      "write": []
    },
    "network": {
      "allowed": false
    }
  },
  "instructions": "Check the artifact of the previous node and conclude.",
  "origin": {
    "type": "native"
  }
}
